apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: ticketing-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: ticketing/auth-service:local
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: ticketing-config
        - secretRef:
            name: ticketing-secrets
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ticketing-local
spec:
  selector:
    app: auth-service
  ports:
  - port: 8000
    targetPort: 8000
