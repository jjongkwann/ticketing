apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: ticketing-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: ticketing/notification-service:local
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: ticketing-config
        - secretRef:
            name: ticketing-secrets
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: ticketing-local
spec:
  selector:
    app: notification-service
  ports:
  - port: 8000
    targetPort: 8000
